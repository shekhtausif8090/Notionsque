// TODO: add zod schemas per endpoint
// export const createTaskSchema = z.object({ ... });
// export const updateTaskSchema = z.object({ ... });
// export const bulkUpdateSchema = z.object({ ids: z.array(z.string().uuid()), patch: z.object({...}) });
// export const reorderSchema = z.array(z.object({ id: z.string().uuid(), position: z.number().int(), priority: z.string() }));
export {};
